# Documentation build requirements for Read the Docs
# These packages are required to build the MkDocs documentation

# Core MkDocs and theme
mkdocs>=1.5.0
mkdocs-material>=9.0.0

# MkDocs plugins used in mkdocs.yml
mkdocs-autorefs>=0.4.1
mkdocstrings[python]>=0.20.0
mkdocs-jupyter>=0.24.0

# Python dependencies for documentation generation
# (These may be needed if your code imports are executed during doc generation)
pandas>=1.3.0
openpyxl>=3.0.0
numexpr>=2.7.0
jinja2>=3.0.0
plotly>=5.0.0
PyYAML>=6.0.0
